## Packages
date-fns | Date formatting for chat messages and timestamps
recharts | Dashboard charts and visualizations

## Notes
- Using periodic polling (refetchInterval) for real-time inbox updates to ensure reliability without relying solely on WebSockets.
- The 3-panel inbox layout uses responsive design: it collapses to a single view on mobile screens.
- Assuming the backend properly handles session cookies for authentication.
